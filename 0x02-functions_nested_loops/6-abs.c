#include "main.h"

/**
 * _abs -returns the absolute value of any num
 *
 * @i: charakter in ascii code
 *
 * Return:absolute value 
 */

int -abs(int i)
{
	if (i >= 0)
{
	return (i);
}
else
{
	return (i * -1);
}
}
